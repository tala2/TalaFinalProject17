<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tala.mubarki.talafinalproject17">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalaFinalProject17">
        <activity android:name=".MyUI.SignUpActivity"></activity>
        <activity android:name=".MyUI.SignInActivity" />
        <activity android:name=".MyUI.HomeScreenActivity" />
        <activity android:name=".MyUI.AddShopActivity" />
        <activity android:name=".MyUI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>