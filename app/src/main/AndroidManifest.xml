<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tala.mubarki.talafinalproject17">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalaFinalProject17">
        <activity android:name=".ProfileActivity"></activity>
        <activity
            android:name=".MyUI.MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.TalaFinalProject17.NoActionBar" />
        <activity android:name=".shopsActivity" />
        <activity android:name=".MyUI.FundamentalActivity" />
        <activity android:name=".PickImageActivity" />
        <activity android:name=".MyUI.SignUpActivity" />
        <activity android:name=".MyUI.SignInActivity" />
        <activity android:name=".MyUI.HomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyUI.AddShopActivity" />
        <activity android:name=".MyUI.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>